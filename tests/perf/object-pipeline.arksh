perf reset

for round in list(1, 2, 3, 4, 5, 6, 7, 8)
do
let ranked = . -> children() |> where(type == "file") |> sort(size desc)
let preview = ranked |> take(12)
let total = preview |> count()
done

perf show
