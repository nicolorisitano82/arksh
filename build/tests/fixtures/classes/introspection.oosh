class Base do
  property name = text("base")
endclass

class Child extends Base do
  property extra = text("x")
endclass

let item = Child()
item -> isa("Base")
item -> type
Child -> methods |> count()
