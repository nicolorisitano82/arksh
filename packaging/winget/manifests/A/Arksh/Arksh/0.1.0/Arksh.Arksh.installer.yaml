# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.4.0.schema.json
# winget installer manifest for arksh 0.1.0
#
# After publishing a GitHub release with a Windows installer/ZIP:
# 1. Replace InstallerUrl with the actual release asset URL.
# 2. Replace InstallerSha256 with the SHA-256 of that asset.
# 3. Submit to https://github.com/microsoft/winget-pkgs via pull request.

PackageIdentifier: Arksh.Arksh
PackageVersion: 0.1.0
MinimumOSVersion: "10.0.17763.0"
InstallerType: zip
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: arksh.exe
    PortableCommandAlias: arksh
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/nicolorisitano82/arksh/releases/download/v0.1.0/arksh-0.1.0-win64.zip
    InstallerSha256: REPLACE_WITH_SHA256_OF_WIN64_ZIP
ManifestType: installer
ManifestVersion: 1.4.0
